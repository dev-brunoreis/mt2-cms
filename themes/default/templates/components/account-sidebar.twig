<nav class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm space-y-1">
    <p class="mb-3 px-2 text-xs font-semibold uppercase tracking-wider text-slate-500">
        {{ t('account.sidebar') }}
    </p>
    <a href="/account"
       class="block rounded-lg bg-indigo-50 px-3 py-2 text-sm font-medium text-indigo-700">
        {{ t('account.dashboard') }}
    </a>
    <a href="/account#characters"
       class="block rounded-lg px-3 py-2 text-sm text-slate-600 hover:bg-slate-50 hover:text-slate-900 transition">
        {{ t('account.characters') }}
    </a>
    <form method="post" action="/logout" class="pt-2">
        <input type="hidden" name="_csrf" value="{{ csrf }}">
        <button type="submit"
                class="w-full cursor-pointer rounded-lg px-3 py-2 text-left text-sm text-red-600 hover:bg-red-50 transition">
            {{ t('nav.logout') }}
        </button>
    </form>
</nav>
