{% if discord_invite_url %}
<div class="k-panel">
    <div class="k-panel-head">{{ t('nav.discord') }}</div>
    <div class="p-3">
        <p class="mb-3 text-xs k-muted">{{ t('theme.kingdoms.discord_lead') }}</p>
        <a href="{{ discord_invite_url }}" target="_blank" rel="noopener noreferrer" class="k-btn w-full">
            {{ t('theme.kingdoms.discord_join') }}
        </a>
    </div>
</div>
{% endif %}
